# Sesi 2026-07-25 — Penyelesaian Sesi 24, Smoke Test Redis & Retry WA

> Status: Session 24 fully complete ✅ | 10 warning → 1 warning ✅ | Lanjut Sub-bab 05
> Lanjutan: [Sesi 26](SESSION-2026-07-26.md)

## Progress Keseluruhan

| Sub-bab | Sebelum | Sesudah | Keterangan |
|---------|:---:|:---:|------|
| Sesi 24 | 90% | 100% | 2 item sisa selesai |
| Web lint | 10 warning | 1 warning | Bersih 9 warning |

---

## 1. Smoke Test Redis Lokal di VM

### Konteks
Redis lokal (container) menggantikan Upstash di sesi 24. Perlu verifikasi Redis berfungsi penuh.

### Hasil

| Level | Test | Hasil |
|:-----:|------|:-----:|
| 1 | `redis-cli PING` | `PONG` |
| 2 | `SET cek_ombak` + `GET cek_ombak` | `halo_dari_session_25` |
| 3 | `curl /health/ready` | `{"db":"ok","redis":"ok"}` |

**Kesimpulan:** Redis lokal berfungsi penuh — koneksi, baca-tulis, AOF persistence, dan roundtrip aplikasi backend via ioredis.

### SSH Key Setup
- Generate RSA 4096-bit key pair di development machine
- Public key ditambahkan ke `~/.ssh/authorized_keys` di VM
- Sekarang bisa SSH non-interaktif: `ssh ubuntu@43.128.98.52`

### Catatan Teknis
- Container backend pakai ESM (`"type": "module"` di package.json)
- `require('ioredis')` dari path `/tmp` gagal karena pnpm structure (`.pnpm/ioredis@5.10.1/`)
- Solusi: file `.cjs` di `/app/`, atau cukup andalkan health endpoint

---

## 2. Retry Manual WhatsApp

### Tinjauan Awal
Dugaan: perlu buat endpoint retry WA dari nol.

### Fakta
Backend **sudah memiliki** semua endpoint yang dibutuhkan:

| Endpoint | Method | Status |
|----------|--------|:---:|
| `GET /admin/wa/failed` | List job gagal | ✅ Sudah ada |
| `POST /admin/wa/retry/:id` | Retry job per ID | ✅ Sudah ada |
| `POST /admin/wa/flush-failed` | Bersihkan semua | ✅ Sudah ada |

### Yang Dikerjakan
**UI web** — tambah daftar failed jobs + tombol retry di halaman WA Monitor.

File: [apps/web/src/app/dashboard/wa-monitor/page.tsx](apps/web/src/app/dashboard/wa-monitor/page.tsx)

Perubahan:
- Interface `FailedJob`, `FailedJobData`, `FailedJobsResponse`
- State: `failedJobs`, `failedJobsLoading`, `retryingIds`
- Fungsi: `fetchFailedJobs()`, `handleRetry(id)`, `handleRetryAll()`, `handleFlushFailed()`
- DLQ section dinamis:
  - Daftar failed jobs (ID, penerima, nominal, alasan gagal, jumlah attempt)
  - Tombol **Retry** per job (spinner saat loading, optimistic remove)
  - Tombol **Retry Semua** (retry semua sekaligus dengan progress)
  - Tombol **Bersihkan Semua** (tetap ada)
  - Empty state (ceklis hijau + "Semua bersih")

### Bull Board
Diputuskan **tidak dipasang**. WA Monitor yang sudah ada + retry UI sudah mencakup semua kebutuhan monitoring. Bull Board hanya berguna untuk 5+ queue dengan puluhan job type.

---

## 3. Pembersihan Lint Warning

### Sebelum: 10 warning, 1 error

| Kategori | Jumlah | Penanganan |
|----------|:---:|------|
| A — Unused imports di WA Monitor | 6 | Hapus import + state tidak terpakai |
| B — `<img>` tanpa next/image | 3 | Migrasi ke `<Image />` dari `next/image` |
| C — Missing useEffect deps | 1 | Dibiarkan (justru stabil, tidak bug) |

### Sesudah: 1 warning

Sisa: `react-hooks/exhaustive-deps` di WA Monitor — sengaja dibiarkan. Menambah fungsi lokal ke dependency array menyebabkan infinite loop karena referensi berubah tiap render.

### File yang Dimodifikasi

| File | Perubahan |
|------|-----------|
| [wa-monitor/page.tsx](apps/web/src/app/dashboard/wa-monitor/page.tsx) | Hapus `useEffect`, `Building2`, `Calendar`, `BarChart`, `Bar`, `branchStats` |
| [login/page.tsx](apps/web/src/app/(auth)/login/page.tsx) | `<img>` → `<Image>`, tambah `import Image from 'next/image'` |
| [Sidebar.tsx](apps/web/src/components/Sidebar.tsx) | `<img>` → `<Image>`, tambah `import Image from 'next/image'` |
| [ProfileCard.tsx](apps/web/src/app/dashboard/users/[id]/ProfileCard.tsx) | `<img>` → `<Image unoptimized>` (URL eksternal Supabase) |

---

## File Temporary Dihapus

- `tmp/smoke_redis.js` — script smoke test Redis
- `tmp/redis_roundtrip.sh` — shell script roundtrip test

---

## Verifikasi

| Command | Hasil |
|---------|:---:|
| `pnpm --filter web typecheck` | ✅ Pass |
| `pnpm --filter web lint` | ✅ 0 error, 1 warning |

---

## Status VM Production

| URL | Status |
|-----|:---:|
| `https://api.lazisnu.site/health/ready` | `{"db":"ok","redis":"ok"}` |
| `https://dashboard.lazisnu.site` | ✅ Login OK |
| VM ↔ Repo | ✅ SSH key siap |
| Container | 5/5 (backend, web, worker, nginx, redis) |

---

## Next Steps

### Dibawa dari Sesi 23 (Belum Selesai)

| # | Item | Keterangan |
|---|------|------|
| 3 | **Sub-bab 05 — Frontend** (0/13 task) | deviceId mobile, cookie maxAge web 15 menit, middleware role guard |
| 4 | **Sub-bab 07C — Observability lanjutan** (4/14, sisa 10) | Uptime Kuma, backup pg_dump, staging env |
| 5 | **Mobile — Rebuild APK** | Domain lazisnu.site di apps/mobile/src/services/api.ts |

### Quick Wins — Sisa Task Sub-bab Hampir 100%

| Sub-bab | Sisa | Detail Task |
|---------|:---:|------|
| 02 — Backend Core | 3 | 02-D2: workers/whatsapp.worker.ts → logger, 02-D4: services/whatsapp.ts → logger |
| 03 — Data Model | 2 | 03-B2: migration immutable-rule.sql, 03-E1: verifikasi enum user_role |
| 04 — Alur Sesi | 3 | 04-A7: unit test 5 skenario, 04-F1/F2: integration test auth |
| 07A — Infra Quick Wins | 1 | CI unit test + /metrics Prometheus |
| 07B — Containerization | 2 | ci.yml job build-image + deploy |

### Urutan Rekomendasi

1. **Quick Wins** (02/03/04 sisa) — 15-60 menit, tutup 3 sub-bab ke 100%
2. **Sub-bab 05 — Frontend** — 0% selesai, 13 task
3. **07C lanjutan** + **Mobile APK**

---

## Lanjutan Sesi 25 — Quick Wins + CI/CD Jobs

> Tanggal: 2026-07-25 (lanjutan sesi 25) | Progress: 81/127 → 90/127 (64% → 71%)

### Hasil

| Sub-bab | Awal | Akhir | Status |
|---------|:---:|:---:|:-----:|
| 02 — Backend Core | 81% | 100% | ✅ |
| 03 — Data Model | 83% | 100% | ✅ |
| 04 — Alur Data & Sesi | 82% | 100% | ✅ |
| 07B — Containerization | 80% | 100% | ✅ |

---

### 5. Sub-bab 02 — Backend Core (02-D2, 02-D4)

**Akar masalah:** Worker dan service masih pakai `console.log`/`console.error`, tidak ter-redact Pino. Di luar konteks Fastify HTTP, tidak dapat logger server.

**Solusi:** Buat shared Pino logger (`src/config/logger.ts`) dengan redact paths yang sama dengan app.ts.

**File yang dibuat/dimodifikasi:**
| File | Perubahan |
|------|-----------|
| [config/logger.ts](apps/backend/src/config/logger.ts) | BARU — shared Pino logger, redact: authorization, password, otp, token |
| [workers/whatsapp.worker.ts](apps/backend/src/workers/whatsapp.worker.ts) | 4 `console.log`/`console.error` → `logger.info()`/`logger.error()` |
| [services/whatsapp.ts](apps/backend/src/services/whatsapp.ts) | 7 `console.log`/`console.error` → `logger.info()`/`logger.error()` |

**Verifikasi:** lint ✅, typecheck ✅, unit test 46/46 ✅

---

### 6. Fix: 3 Integration Test Fail (collectionSubmission)

**Akar masalah:** Test tidak idempotent — `beforeAll` insert district `DTI`, `afterAll` gagal cleanup jika crash/timeout.

**Solusi:** Tambah cleanup block di awal `beforeAll` (child → parent: collections → assignments → cans → officers → users → branches → districts).

**File:** [collectionSubmission.integration.test.ts](apps/backend/src/services/__tests__/collectionSubmission.integration.test.ts)

**Masalah kedua:** Test DB `lazisnu_test` tidak punya kolom `fcm_token` dan `device_id` — `drizzle-kit push` tidak mendeteksi. Fix: ALTER TABLE manual.

**Hasil:** 49/49 test PASS (5 suite).

---

### 7. Sub-bab 03 — Data Model (03-B2, 03-E1)

**03-B2 — Migration immutable-rule.sql:**

**Akar masalah:** `immutable-rule.sql` tidak terdaftar di `_journal.json` Drizzle — deployment ke env baru akan skipped.

**Solusi:** Buat migration resmi `0004_immutable_rule.sql` + entry journal + hapus file lama.

**File:**
| File | Perubahan |
|------|-----------|
| [0004_immutable_rule.sql](apps/backend/src/database/migrations/0004_immutable_rule.sql) | BARU — CREATE OR REPLACE RULE (idempotent) |
| [_journal.json](apps/backend/src/database/migrations/meta/_journal.json) | Tambah entry `0004_immutable_rule` |
| `immutable-rule.sql` (lama) | DIHAPUS |

**Validator orphan:** Dibuat `scripts/check-migration-orphans.js` — deteksi file SQL tidak terdaftar di journal. Ditambahkan sebagai `db:check-orphans` script + step CI.

**03-E1 — Verifikasi enum user_role:**

**Akar masalah:** PostgreSQL enum bisa punya nilai yang sudah dihapus dari kode TypeScript (`ADMIN_PUSAT`, `ADMIN_KABUPATEN`).

**Hasil:** Query `enum_range(NULL::user_role)` di Supabase (production) dan test DB — keduanya hanya 4 nilai valid. Tidak ada role hantu.

---

### 8. Sub-bab 04 — Alur Data & Sesi (04-A7, 04-E1, 04-F1, 04-F2)

**04-E1 — Redis eviction policy:** Ditandai selesai — `volatile-lru` sudah di `redis/redis.conf` (sesi 24).

**04-A7 — Unit test tokenService (5 skenario):**

Dibuat [tokenService.test.ts](apps/backend/src/services/__tests__/tokenService.test.ts):
| # | Skenario | Hasil |
|---|----------|:---:|
| 1 | Login device baru — key Redis + TTL 365 hari | ✅ |
| 2 | Login ulang device sama — key tertimpa, 1 key | ✅ |
| 3 | Rotasi refresh — jti lama invalid, baru valid | ✅ |
| 4 | Revoke 1 sesi — device itu mati, lain hidup | ✅ |
| 5 | Revoke all — semua mati kecuali current | ✅ |

Menggunakan `ioredis-mock` (otomatis saat REDIS_URL tidak diset di test).

**04-F1/F2 — Integration test auth (3 skenario):**

Ditambahkan ke [auth.integration.test.ts](apps/backend/src/routes/__tests__/auth.integration.test.ts):
| # | Skenario | Hasil |
|---|----------|:---:|
| 1 | Full cycle: login → refresh → logout → refresh gagal | ✅ |
| 2 | Revoke single → REFRESH_REVOKED, device lain tetap hidup | ✅ |
| 3 | Revoke all → semua mati kecuali current | ✅ |

**Catatan:** Test lulus saat dijalankan terisolasi (`jest -t "Session Management"`). 2 dari 3 test gagal saat dicampur dengan outer describe block — test pollution dari `jest.mock` global. Dicatat sebagai Technical Debt TD-01 di master list.

---

### 9. Sub-bab 07B — Containerization (07-B2-1, 07-B2-2)

**07-B2-1 — CI job `build-image`:**

Ditambahkan ke [ci.yml](.github/workflows/ci.yml):
- Build backend + web Docker image
- Push ke GHCR: `ghcr.io/lazisnu/backend:latest` + `:sha`, `ghcr.io/lazisnu/web:latest` + `:sha`
- Hanya run di push/main (bukan PR)
- Cache: GitHub Actions cache

**07-B2-2 — CI job `deploy`:**

- SSH ke VM Tencent Cloud (appleboy/ssh-action)
- `docker login` GHCR
- Pull images terbaru
- `docker compose -f docker-compose.prod.yml down && up -d`
- Health check: `curl /health` + `curl /`

**Dibuat:** [docker-compose.prod.yml](docker-compose.prod.yml) — pakai pre-built images GHCR, tanpa `build:`.

**Prasyarat (sebelum bisa jalan):**
| Secret GitHub | Nilai |
|---|---|
| `VM_HOST` | IP Tencent Cloud |
| `VM_USER` | `lazisnu` |
| `VM_SSH_KEY` | Private key (sudah dibuat di sesi 25) |
| `vars.NEXT_PUBLIC_API_URL` | `https://api.lazisnu.site` |

**Alur CI/CD sekarang:**
```
Push main → lint-and-typecheck → build-image (GHCR) → deploy (VM)
```

---

### Technical Debt Dicatat

Ditambahkan section Technical Debt di MASTER-GOAL-LIST.md:
- **TD-01:** auth.integration.test.ts — mock DB global bocor antar describe block
- **TD-02:** collectionSubmission.integration.test.ts — test tidak idempotent (sudah difix)
- **TD-03:** Test DB tidak auto-sync dengan Drizzle schema

---

### Verifikasi Akhir

| Command | Hasil |
|---------|:---:|
| `pnpm lint` (tsc --noEmit) | ✅ |
| `pnpm test:unit` (6 suite) | ✅ 54/54 |
| `pnpm test:unit -t "tokenService"` (5 skenario) | ✅ |
| `pnpm test:unit -t "Session Management"` (3 skenario) | ✅ |

---

*Sesi 25 — melanjutkan dan menyelesaikan sesi 24.*

*Bersambung ke [Sesi 26](SESSION-2026-07-26.md) — Sub-bab 05 Frontend Web & Mobile.*
