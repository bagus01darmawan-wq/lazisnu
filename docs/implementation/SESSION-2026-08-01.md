# Sesi 2026-08-01 — Penelusuran Backlog + Uptime Kuma Lapis 3-4 Deploy ✅ + CI Upgrade Actions ke Node 24 ✅

> Status: Lapis 3-4 SELESAI (backup R2 + compose migration + status page live) | TASK-PENDING.md dibuat sebagai single source of truth | **Bagian 2: CI GitHub Actions upgrade ke Node 24 SELESAI (Batch A+B merged, Gate 1-3 lulus, 0 annotation deprecation, staging OK)**

---

## Konteks

Sesi sebelumnya (2026-07-31, Sesi 32) menuntaskan CI deploy-staging. Sesi ini berisi 2 pekerjaan:

1. **Penelusuran item pending** dari SESSION-2026-07-23 s/d 29 + pembuatan file ringkasan `TASK-PENDING.md`
2. **Eksekusi bagian A** TASK-PENDING — Uptime Kuma Lapis 3-4 (deploy ke VM)

---

## 1. Penelusuran Sesi 23-29 & Pembuatan TASK-PENDING.md

### Hasil Penelusuran

- Mayoritas item sesi 23-29 sudah dituntaskan di sesi 27-30 (08-C3, 08-D1, G3-5, EAS build, dll)
- **Sesi 28 tidak ada file dokumentasi** — gap, tapi tidak ada task yang hilang
- Item yang masih relevan:
  - **F1**: `JWT_SECRET` placeholder di `.env.staging` line 17 masih `staging-legacy-deprecated-key-replace-soon` (dari Sesi 27) — security, staging memakai secret publik
  - **F2**: Verifikasi CI secrets GitHub (VM_HOST, VM_USER, VM_SSH_KEY, NEXT_PUBLIC_API_URL) — de facto terpenuhi (CI deploy-staging jalan), tinggal konfirmasi
  - **F3**: Catatan kosmetik Sesi 27 (BullMQ noeviction + orphan warning) — sudah teratasi

### Koreksi Penting (anti-double-work)

| Item | Status sebenarnya | Bukti |
|------|-------------------|-------|
| TD-04 (request-otp mock) | ✅ SUDAH SELESAI (Sesi 31, commit `bc009a7`) | MASTER-GOAL-LIST line 323 |
| TD-05 (reset rate limit) | ✅ SUDAH SELESAI | MASTER-GOAL-LIST line 324 |
| 07-B2-3 (image GHCR) | ✅ de facto terpenuhi via CI | Sesi 32 |
| Password Grafana | 🚫 SKIP (keputusan user) | Sesi 30 Lanjutan 8 |

### File Baru

**`docs/implementation/TASK-PENDING.md`** — single source of truth berisi 6 bagian:
- A. Uptime Kuma Lapis 3-4 (kategori utama, dieksekusi sesi ini)
- B. Housekeeping VM (stop container lama blue/green)
- C. Disk cleanup Phase 1,3,4,5,6
- D. VERIFICATION-PENDING runtime (~12 item)
- E. Anti-double-work (sudah selesai / skip)
- F. Temuan Sesi 23-29 (JWT_SECRET placeholder, CI secrets)

---

## 2. Eksekusi — Uptime Kuma Lapis 3-4 (Deploy ke VM)

### 2.1 Pre-flight (SSH `lazisnu`)

| Cek | Hasil |
|-----|-------|
| Container `uptime-kuma` | Up 35 jam (healthy) — ⚠️ port terekspos `0.0.0.0:3002` (publik) |
| R2 credentials | ✅ 4 var terisi |
| `awscli` | ✅ ada (`sqlite3` tidak — script fallback ke `docker cp`) |
| Repo VM | ✅ HEAD `fb411fd` = repo lokal (kode Lapis 3-4 sudah ikut ter-pull) |
| `kuma.db` | 2.2MB — kecil, migrasi aman |

### 2.2 Lapis 4a — Backup kuma.db ke R2 ✅

**Bug ditemukan di `scripts/backup-kuma.sh`**:
- `log "START..."` dipanggil **sebelum** `mkdir -p "$BACKUP_DIR"` → `tee` gagal (dir belum ada) → `set -e` exit 1
- **Fix**: pindah `mkdir -p` sebelum log pertama

**Hasil test manual (1x)**:
- `kuma.db` 2.2MB → gzip 456K → upload ke `s3://lazisnu-backups/kuma/20260801/kuma_20260801_004504.db.gz` ✅
- Crontab ditambah: `0 3 * * * bash /opt/lazisnu/scripts/backup-kuma.sh` (jam 03:00, 1 jam setelah backup Postgres)

### 2.3 Lapis 3 — Migrasi ke docker-compose ✅

Urutan: stop container lama → copy volume `uptime-kuma-data` → bind mount `/opt/lazisnu/uptime-kuma/data` → chown → rm container lama → `docker compose up -d uptime-kuma`

**2 bug ditemukan & diperbaiki (di `docker-compose.yml`)**:

| Bug | Gejala | Root Cause | Fix |
|-----|--------|-----------|-----|
| Restart loop | Container exit 1 terus | `user: "1000:1000"` — image Kuma startup `chown` ke node:node butuh **root/CAP_CHOWN**, non-root tidak bisa → `set -e` exit | Hapus `user:` directive (default root; image chown sendiri ke 1000:1000) |
| Healthcheck gagal | Status `unhealthy` | Image Kuma **tidak punya `wget`** (punya `curl`) | Ganti healthcheck `wget --spider` → `curl -f -s -o /dev/null` |

**Hasil**: container healthy, port kini `127.0.0.1:3002->3001` (localhost only ✅ — memperbaiki eksposur publik), 4 monitor + 4 binding intact (verified via query SQLite).

### 2.4 Lapis 4b — Status Page ✅

**Temuan penting: DNS domain di Hostinger, bukan Cloudflare**
- NS domain `lazisnu.site` = `atlas/hyperion.dns-parking.com` (Hostinger) — SOP lama salah tulis Cloudflare
- User menambahkan A record manual: `status` → `43.128.98.52`

**Enable Status Page tanpa UI** (butuh login Kuma yang tidak tersedia):
- Struktur Kuma 1.23.17: `status_page` → `group` (public, status_page_id) → `monitor_group` (relasi monitor)
- Script baru **`scripts/kuma-setup-status-page.sh`** — idempotent (delete-by-slug + insert), ikut pola `kuma-setup-monitors.sh`
- Bug saat eksekusi: kolom `status_page.icon` NOT NULL → set `/icon.svg` (default image)
- Hasil: status page id=1 slug `lazisnu`, group `Lazisnu Status`, 4 monitor terhubung

**SSL (certbot)**:
- nginx container lama masih baca **inode nginx.conf lama** (0 hit status blocks) → reload/restart tidak cukup, harus **recreate container**
- Urutan aman: stop nginx → certbot standalone (`-d status.lazisnu.site`) → `docker compose up -d nginx` (container baru + cert sudah ada)
- Cert expires **2026-10-29**

**Verifikasi akhir**:

| Check | Hasil |
|-------|-------|
| `https://status.lazisnu.site/status/lazisnu` | ✅ HTTP 200, title "Lazisnu Status" |
| API status page | ✅ 1 group, 4 monitor (Lazisnu API, Web, Staging API, Web) |
| Badge | ✅ "Up" |
| Smoke layanan lain (api/dashboard/staging) | ✅ 200/307/200 — tidak terganggu |
| Webhook Discord test | ✅ HTTP 204 (pesan terkirim) |

---

## 3. Koreksi SOP (dari bonus temuan)

| File | Koreksi |
|------|---------|
| `docs/SOP-UPTIME-KUMA.md` | (1) DNS Hostinger bukan Cloudflare (3 tempat); (2) peringatan inode lama — recreate bukan restart; (3) certbot via standalone (bukan `--nginx`); (4) Step 5 cleanup: hapus `docker rm uptime-kuma`, nama volume benar `uptime-kuma-data` |
| `docs/SOP-MOUNT-BIND-FILE.md` | Section 5.5 diperluas: kasus inode lama (diagnosa `grep -c` container vs host; solusi recreate `up -d` bukan restart; peringatan resource belum ada sebelum recreate) |

---

## 3.1 Housekeeping VM — Stop Container Lama Blue/Green (Backlog #4)

**Konteks**: 3 generasi container backend/web/worker hidup berdampingan sejak blue-green deploy (Sesi 30 08-C3). Upstream aktif → blue, jadi container lama `lazisnu-*-1` sudah idle.

**Eksekusi**:
- `docker stop lazisnu-backend-1 lazisnu-web-1 lazisnu-worker-1` → semua Exited (backend/worker exit 0, web exit 143)
- ⚠️ Tidak `docker compose down` penuh (redis/prometheus/grafana satu project) — dipatuhi

**Verifikasi**: api 200, api-health 200, dashboard 307, status 200, staging-api 200 — tidak ada gangguan. Blue aktif, green tetap cadangan rollback (image GHCR terbaru), semua infra jalan.

---

## 3.2 Disk Cleanup (Backlog #5) — Phase 1, 4, 5, 6 ✅ (Phase 3 SKIP)

> Keputusan user: **tidak ingin menghapus unused images (Phase 3)**. Yang lain dieksekusi.

| Phase | Hasil |
|-------|-------|
| 1 — Safe cleanup | journal vacuum 224M→50M (freed 152M); scratch + `penggalan riwayat.txt` dihapus (backup `/tmp/cleanup-2026-07-30/`); `nopeAGENTS.md` di-keep; sysctl `overcommit_memory=1`; apt autoremove dry-run 0 package (skip); placeholder 20-byte dihapus |
| 4 — Backup retention | `scripts/backup.sh`: KEEP_DAYS 30→90 + archive ke R2 prefix `archive/` sebelum hapus lokal (adaptasi `aws s3` — rclone tidak ada) |
| 5 — Move env | `.env.backup` + `.env.backup-2026-07-24` → `secrets/` (700/600), backup R2 dulu (2 file di `s3://lazisnu-backups/secrets/`) |
| 6 — SOP | `docs/SOP-HOUSEKEEPING-VM.md` dibuat |

**Insiden & temuan**:
1. **CRLF bug**: backup.sh di-edit di Windows → `\r` → bash di VM error `syntax error near $'{\r'`. Fix: `sed -i 's/\r$//'` di VM.
2. **Temuan kritis (Phase 5)**: `.env.backup` ternyata credentials AKTIF yang di-source `backup.sh`/`backup-kuma.sh`. Setelah move, cron akan gagal load → dibuat **symlink** `/opt/lazisnu/.env.backup → secrets/env.backup-2026-07-27`. Terverifikasi: backup-kuma test 2x sukses.
3. **Glob over-delete (Phase 1.5)**: glob `lazisnu_20260727_*` menghapus juga 2 backup valid 110KB (bukan hanya <1KB). Dampak minimal — backup 28-31 ada + R2 punya salinan.
4. **Dotfile chmod**: `.env.backup-2026-07-24` tidak kena `chmod 600` karena glob `*` tidak match dotfile → fix manual.

**Verifikasi akhir**: disk 86%→85% (avail 2.8→2.9G), api 200, dashboard 307, status 200, 15 container running, Prometheus OK.

**Audit keamanan 4 temuan (setelah eksekusi)**:
| Temuan | Status | Detail |
|--------|--------|--------|
| 1. `.env.backup` credentials aktif | ✅ AMAN | Symlink valid, `backup.sh` + `backup-kuma.sh` test sukses setelah move (upload R2 OK) |
| 2. CRLF bug | ✅ AMAN (pencegahan) | `.gitattributes` `*.sh text eol=lf` ditambahkan (baru, belum di-commit) + `backup.sh` lokal di-normalisasi LF — cegah kambuh karena VM `core.autocrlf=true` |
| 3. Glob over-delete | ⚠️ 1 file hilang permanen | `lazisnu_20260727_095912.sql.gz` (110KB) TIDAK ada di R2 (hanya `_100031` ter-upload). Dampak minimal: backup 28-31 + R2 salinan lain ada. **Tidak bisa dipulihkan** |
| 4. Dotfile chmod | ✅ AMAN | `.env.backup-2026-07-24` sekarang 600 (fix manual) |

---

## 4. Pending yang Tersisa

| Item | Detail | Kapan |
|------|--------|-------|
| Commit + push perubahan kode | backup-kuma.sh, docker-compose.yml, kuma-setup-status-page.sh, 2 SOP, TASK-PENDING.md, SESSION-2026-08-01.md, backup.sh, .gitignore, SOP-HOUSEKEEPING-VM.md | User pilih "nanti saja" |
| Cleanup volume named | `docker volume rm uptime-kuma-data` (~2.7MB duplikat) — ⚠️ JANGAN `docker rm uptime-kuma` (container lama sudah dihapus; nama itu dipakai container compose aktif) | ≥ 2026-08-02 00:00 WIB (24 jam setelah start 2026-08-01 00:00 WIB) |
| ~~Test alert DOWN asli~~ | ✅ **SELESAI** — TEST-DOWN url mati → DOWN (ECONNREFUSED) → alert Discord masuk 00:26 WIB → monitor temp dihapus, 4 monitor asli utuh | — |
| F1 | Ganti `JWT_SECRET` placeholder staging → random | 5 menit |
| F2 | Konfirmasi CI secrets GitHub | 1 menit |
| ~~B (stop container lama blue/green)~~ | ✅ **SELESAI** — `docker stop lazisnu-backend-1 lazisnu-web-1 lazisnu-worker-1` → dihapus via `docker rm`, smoke test publik 200/307 tanpa gangguan, rollback via green tetap ada | — |
| ~~C (disk cleanup)~~ | ✅ **SELESAI** — Phase 1 (journal 152M, scratch, sysctl), 4 (retention 90d + R2 archive), 5 (secrets + symlink), 6 (SOP). **Phase 3 SKIP** (keputusan user) | — |

---

## File yang Dibuat/Diubah

### Dibuat
| File | Deskripsi |
|------|-----------|
| `docs/implementation/TASK-PENDING.md` | Single source of truth item pending (6 bagian) |
| `scripts/kuma-setup-status-page.sh` | Idempotent setup status page Kuma (status_page + group + monitor_group) |
| `docs/SOP-HOUSEKEEPING-VM.md` | SOP housekeeping bulanan/kuartalan (Phase 6) |

### Diubah
| File | Perubahan |
|------|-----------|
| `scripts/backup-kuma.sh` | `mkdir -p` dipindah sebelum `log START` (fix tee gagal) |
| `docker-compose.yml` | Hapus `user:` uptime-kuma; healthcheck `wget` → `curl` |
| `scripts/backup.sh` | KEEP_DAYS 30→90 + archive R2 prefix `archive/` + placeholder cleanup (Phase 4) |
| `.gitignore` | Tambah `.env.backup*` dan `**/.env.backup*` (Phase 5.5) |
| `docs/SOP-UPTIME-KUMA.md` | DNS Hostinger, inode lama/recreate, certbot standalone, cleanup volume |
| `docs/SOP-MOUNT-BIND-FILE.md` | Section 5.5 kasus inode lama + recreate |
| `docs/implementation/TASK-PENDING.md` | Bagian A/B/C ditandai SELESAI + catatan eksekusi |

---

## Pelajaran

1. **Uptime Kuma image tidak boleh di-set `user:`** — startup `chown` butuh root. Berlaku beda dari redis/prometheus/grafana (Sesi 30 Lanjutan 5).
2. **Bind-mount file + container lama = inode lama** — `restart`/`reload` nginx tidak cukup; butuh recreate container. Dan pastikan resource yang direferensikan config (cert) sudah ada SEBELUM recreate.
3. **SOP yang ditulis tanpa eksekusi nyata bisa salah asumsi** — DNS ternyata Hostinger (bukan Cloudflare), image punya curl bukan wget, volume named bernama `uptime-kuma-data`. Eksekusi langsung mengungkap kebenaran.
4. **`docker volume rm` gagal dengan "volume in use"** jika masih dipakai — tunggu 24 jam + pastikan container compose sudah pakai bind mount.

---

## 5. Bagian 2 — CI GitHub Actions Upgrade ke Node 24 (Review Rencana + Eksekusi Penuh) ✅

> Rencana: `docs/implementation/RENCANA-V2-CI-ACTION-UPGRADE.md` (dibuat dari draft `RENCANA-CI-ACTION-UPGRADE.md`, direview, disetujui, dan dieksekusi di sesi ini)

### 5.1 Review rencana — 2 koreksi faktual kritis

1. **§3.3 Opsi C draft lama SALAH FAKTUAL**: draft menulis "hapus `pnpm/action-setup`, `actions/setup-node@v5` auto-install pnpm dari `packageManager`". **Kenyataan**: setup-node v5/v6 **tidak pernah meng-install pnpm** — fitur barunya hanya **auto-cache** (PR actions/setup-node#1348), README eksplisit *"Package manager should be pre-installed"*. Jika dieksekusi → CI merah (`pnpm: command not found`). Koreksi: pin `pnpm/action-setup@v5.0.0` (atau `corepack enable`).
2. **§3.4 setup-node v6 sudah rilis** (PR #1374): auto-cache dibatasi **hanya npm**. Plan dikunci ke **v5** (bukan "versi terbaru"), dengan 2 konfigurasi caching valid tertulis eksplisit.
3. Deadline dikoreksi: Node 20 dihapus dari runner **16 September 2026** (tertulis di teks warning Annotation), bukan perkiraan "Fall 2026".

**Verifikasi silang**: audit §2 cocok 100% dengan ci.yml; masukan user tentang `checkout@v5` di L133/L172 divalidasi — v4→v5 murni bump runtime node24 tanpa breaking change; upgrade keempat `checkout` sekaligus di Phase 1 benar (risiko L133 deploy tanpa jalur uji dini diterima).

### 5.2 Persetujuan & keputusan D1-D5 (semua sesuai rekomendasi)

| Keputusan | Pilihan |
|-----------|---------|
| D1 | (a) Segera |
| D2 | (a) Pin `pnpm/action-setup@v5.0.0` |
| D3 | (a) Auto-cache v5 (hapus `cache:` eksplisit) |
| D4 | (a) Upgrade `docker/build-push-action@v7.3.0` + uji |
| D5 | (a) Aktifkan Dependabot untuk GitHub Actions |

### 5.3 Eksekusi Batch A — PR #18 (`ci/upgrade-actions-node24`)

Perubahan ci.yml: `checkout@v4`→`@v5` (×4), `pnpm/action-setup@v3`→`@v5.0.0`, `setup-node@v4`→`@v5` + hapus `cache: 'pnpm'` (auto-cache).

**Temuan nyata (di luar plan)**: `pnpm/action-setup@v5.0.0` **menolak dua sumber versi sekaligus** (`with: version: 10` + `packageManager` di package.json) → `ERR_PNPM_BAD_PM_VERSION`, CI fail 21s. Fix (commit `538d666`): hapus `version: 10` → pnpm deterministik **10.33.2** dari `packageManager` — justru lebih sesuai prinsip pin versi.

**Gate 1 LULUS** (run `30660843317`): lint-and-typecheck pass 1m55s, 54/54 unit test, auto-cache v5 **cache hit**, urutan setup benar (`pnpm store path` ter-resolve), annotation deprecation Batch A hilang → **merged**.

### 5.4 Eksekusi Batch B — PR #19 (`ci/upgrade-docker-actions`)

Perubahan ci.yml: `docker/login-action@v3`→`@v4`, `docker/setup-buildx-action@v3`→`@v4`, `docker/build-push-action@v6`→`@v7` (×2 backend+web). Commit `c4f1237`.

Uji via **workflow_dispatch dari branch** (run `30661900400`): lint pass 1m43s, build-image pass 2m48s, image `backend`+`web` `:latest`+`:c4f1237` ter-push ke GHCR, attestation/provenance OK, deploy/deploy-staging skip (benar — bukan main/tag), **0 annotation** → **Gate 2 LULUS & merged**.

### 5.5 Verifikasi end-to-end pasca-merge — Gate 3 LULUS

Run main final `30662371949` (push main): lint-and-typecheck ✅ (1m43s) · build-image ✅ (3m31s) · deploy-staging ✅ (54s) · deploy skip ✅ · **0 annotation deprecation** ✅. Staging: `staging-api` **200**, `staging-web` **307**. Production tidak tersentuh.

### 5.6 Phase 4 — Dokumentasi & backlog

- `.github/dependabot.yml` dibuat (ecosystem `github-actions`, `/`, mingguan) — commit `4764fa6` di main
- `TASK-PENDING.md` bagian E + entry "CI GitHub Actions upgrade ke Node 24 ✅ SELESAI"
- `RENCANA-V2-CI-ACTION-UPGRADE.md`: semua fase & Gate 0-3 dicentang LULUS

### 5.7 File yang Diubah/Dibuat

| File | Perubahan |
|------|-----------|
| `.github/workflows/ci.yml` | Batch A (checkout v5, pnpm v5.0.0, setup-node v5 + auto-cache) + Batch B (docker v4/v4/v7) |
| `.github/dependabot.yml` | **Baru** — Dependabot GitHub Actions mingguan |
| `docs/implementation/RENCANA-V2-CI-ACTION-UPGRADE.md` | **Baru** — rencana final (review + keputusan + status eksekusi) |
| `docs/implementation/TASK-PENDING.md` | Entry selesai di bagian E |

### 5.8 Pelajaran

1. **`pnpm/action-setup` v5 = single source of truth** — tidak boleh ada `version:` + `packageManager` bersamaan (`ERR_PNPM_BAD_PM_VERSION`). v3 tidak memeriksa ini; v5 memeriksa.
2. **setup-node v5 ≠ v6** untuk caching pnpm: v5 auto-cache semua package manager, v6 hanya npm (PR #1374). Upgrade ke v6 nanti wajib restore `cache: 'pnpm'` eksplisit + `package-manager-cache: false`.
3. **Edit paralel (SearchReplace) pada file yang sama bisa saling menimpa** — hanya edit terakhir yang persist (terjadi 2×: ci.yml dan dokumen rencana). Untuk file sama: edit **sekuensial**.
4. Deadline nyata migrasi = **16 September 2026** (tertulis di warning annotation), bukan "Fall 2026" — upgrade selesai jauh sebelum itu (1 Agustus).

---

*Sesi 2026-08-01 selesai (2 bagian): (1) Uptime Kuma Lapis 3-4 live end-to-end — backup harian R2 (cron 03:00), container di compose (localhost-only), status page publik `status.lazisnu.site` + SSL, webhook Discord terverifikasi, housekeeping VM + disk cleanup; (2) CI GitHub Actions upgrade ke Node 24 tuntas — Batch A+B merged (PR #18/#19), Gate 1-3 lulus, 0 annotation deprecation di semua job, staging 200/307, Dependabot aktif. TASK-PENDING.md + RENCANA-V2-CI-ACTION-UPGRADE.md jadi acuan.*
