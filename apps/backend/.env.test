NODE_ENV=test
PORT=3002
API_BASE_URL=http://localhost:3002

# PostgreSQL connection string for test db
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lazisnu_test

# Redis (Dihapus agar menggunakan ioredis-mock secara otomatis dalam testing)
# REDIS_URL=redis://localhost:6379

# JWT — harus minimal 32 karakter
JWT_SECRET=super_secret_test_key_for_lazisnu_1234
JWT_EXPIRES_IN=1h

# App Secret untuk QR Signing — harus minimal 32 karakter
APP_SECRET=test_app_secret_for_qr_signing_lazisnu

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8081

